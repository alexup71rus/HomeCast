# HomeCast

**HomeCast** — это высокопроизводительное решение для дублирования экрана и звука с Android устройства на веб-браузер с минимальной задержкой.

## 🚀 Особенности

*   📺 **Видео**: MJPEG поток (30-60 FPS) с адаптивным качеством и низкой задержкой.
*   🔊 **Аудио**: Захват системного звука (Android 10+ API 29), сжатие **G.711 u-Law** (снижение трафика на 50%).
*   ⚡ **Протокол**: Оптимизированный бинарный протокол поверх WebSocket (Raw Binary) вместо JSON/Base64.
*   🎨 **Дизайн**: Современный интерфейс "Candy Dark" на Flutter.
*   ⚙️ **Гибкость**: Настройка FPS, качества JPEG и аудио-буфера "на лету" из приложения.

## 📂 Структура проекта

*   **`/App`** — Мобильное приложение (Flutter + Native Hello-World code).
    *   `lib/` — UI и логика на Dart.
    *   `android/` — Нативный код захвата экрана и звука (Kotlin).
*   **`/Web`** — Веб-клиент и Relay-сервер.
    *   `public/viewer.js` — Кастомный плеер с джиттер-буфером и декодером u-Law.
    *   `server.js` — Node.js сервер для статики и сопряжения (опционально).

## 🛠 Установка и запуск

### Предварительные требования
*   **Flutter SDK** (3.x+)
*   **Node.js** (14+)
*   **Android Device** (Android 10+)

### 1. Запуск Веб-части (По желанию)
Приложение может работать и без внешнего сервера (прямое подключение по IP), но сервер удобен для хостинга `viewer.html` и DeepLink магии.

```bash
cd Web
npm install
node server.js
```
Откройте браузер по адресу: `http://localhost:3000`

### 2. Сборка и запуск Приложения

```bash
cd App
flutter pub get
flutter run --release
```
*Рекомендуется запускать в `--release` (или `--profile`) режиме для реальной производительности.*

## 🎨 Кастомизация

### Иконка приложения
Файлы иконок находятся в директории:
`App/android/app/src/main/res/`
Ищите папки `mipmap-mdpi`, `mipmap-hdpi` и т.д. Замените `ic_launcher.png` на свой дизайн.

### Настройки подключения
Адрес бэкенда для диплинков можно задать в `App/assets/config.json`.

## 🔧 Технический стек

*   **Mobile**: Flutter, Kotlin, MediaProjection API, AudioPlaybackCaptureConfiguration.
*   **Protocol**: Custom Binary WebSocket (Header [1 byte Type] + Payload).
*   **Web**: Vanilla JS, Web Audio API, Canvas 2D (BitmapRenderer).

---
Developed with ❤️ by **[alexup71rus](https://github.com/alexup71rus)** & **GitHub Copilot**
